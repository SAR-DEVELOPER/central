# Development Environment Configuration

# Environment Mode
NODE_ENV=development

# Docker Build Targets
BACKEND_TARGET=development
FRONTEND_TARGET=development

# Port Configuration
BACKEND_PORT=4000
FRONTEND_PORT=3000

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000

# PostgreSQL Database Configuration
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev_postgres_password
POSTGRES_DB=centri_db_dev

# MongoDB Database Configuration
MONGODB_PORT=27017
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=dev_mongo_password
MONGO_DB=centri_db_dev

# Database Application Users (for direct connection)
# PostgreSQL App User: centri_app / dev_postgres_app_password
# MongoDB App User: centri_app / dev_mongo_app_password
